<?xml version="1.0" encoding="utf-8"?>
<!--
     SPDX-FileCopyrightText: The LineageOS Project
     SPDX-License-Identifier: Apache-2.0
-->
<resources>
    <!-- Doze: does this device support STATE_DOZE?  -->
    <bool name="doze_display_state_supported">true</bool>

    <!-- Override value to use for proximity sensor.  -->
    <string name="proximity_sensor_type" translatable="false">xiaomi.sensor.oem_pocket</string>

    <!-- If using proximity_sensor_type, specifies a threshold value to distinguish near and
         far break points. A sensor value less than this is considered "near". -->
    <item name="proximity_sensor_threshold" format="float" type="dimen">5</item>

    <!-- Should we vibrate on an icon animation of the shelf. This should only be active if the
     vibrator is capable of subtle vibrations -->
    <bool name="config_vibrateOnIconAnimation">false</bool>

    <!-- Preferred refresh rate at keyguard, if supported by the display. Overrides
         keyguardMaxRefreshRate. -->
    <integer name="config_keyguardRefreshRate">120</integer>
    <!-- BestROM: a leaner default Quick Settings set. Everything else stays available in the tile editor. -->
    <string name="quick_settings_tiles_default" translatable="false">
        internet,bt,flashlight,dnd,rotation,battery,screenrecord,airplane
    </string>

    <!-- BestROM: keep the status bar quiet by default. -->
    <string-array name="config_statusBarIconsToExclude" translatable="false">
        <item>@*android:string/status_bar_rotate</item>
        <item>@*android:string/status_bar_headset</item>
        <item>@*android:string/status_bar_alarm_clock</item>
        <item>@*android:string/status_bar_bluetooth</item>
        <item>@*android:string/status_bar_vpn</item>
    </string-array>
</resources>
